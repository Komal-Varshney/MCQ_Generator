# This src : we are gonna use it as our local folder
# mcq_generator : this folder is considered as package
# package : a folder containing multiple python files containng the code
# and if we want to treat any folder as local package then there is a convention in python that we have to mention __init__.py file in that folder
